# Gerado automaticamente por nixos-generate-config. Substitua pelo hardware real da máquina.
{ config, lib, pkgs, modulesPath, ... }: {
  imports = [
    (modulesPath + "/installer/scan/not-detected.nix")
  ];

  # TODO: preencher com saída de: nixos-generate-config --show-hardware-config
}
