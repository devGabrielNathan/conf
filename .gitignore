# Segredos do sistema — NUNCA commitar
secrets/
*.password
*.hashed

# Assets locais de inspiração
inspiration/

# Configuração específica da máquina — gerada pelo wizard
hosts/*/hamra.json
hosts/*/hamra-config.nix

# Pode ser versionada se o repo for privado; descomente para ignorar em repos públicos:
# hosts/*/hamra.nix
# hosts/*/hardware-configuration.nix

# Arquivos de backup temporários
*.bak